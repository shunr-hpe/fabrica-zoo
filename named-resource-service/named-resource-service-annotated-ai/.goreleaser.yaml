
# Generated by Fabrica dev
# SPDX-FileCopyrightText: Copyright © 2026 OpenCHAMI a Series of LF Projects, LLC
# SPDX-License-Identifier: MIT

version: 2

before:
  hooks:
    - go mod download

builds:
  - id: named-resource-service
    main: ./cmd/server
    binary: named-resource-service
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    # Embed version information via ldflags
    ldflags:
      - -s -w
      - -X main.serviceVersion={{.Version}}
      - -X main.serviceCommit={{.Commit}}
      - -X main.serviceDate={{.Date}}
    flags:
      - -trimpath

archives:
  - id: named-resource-service
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
    files:
      - LICENSE*
      - README.md
      - docs/**/*

checksum:
  name_template: 'checksums.txt'
  algorithm: sha256

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^ci:'
      - '^chore:'
      - Merge pull request
      - Merge branch

release:
  github:
    owner: {{ if .Env.GITHUB_OWNER }}{{ .Env.GITHUB_OWNER }}{{ else }}your-org{{ end }}
    name: named-resource-service
  draft: false
  prerelease: auto
